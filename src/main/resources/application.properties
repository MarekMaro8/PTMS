spring.application.name=PTMS
# ----------------------------------------------------
# Ustawienia Po??czenia JDBC (PostgreSQL)
# ----------------------------------------------------
spring.datasource.url=jdbc:postgresql://localhost:5432/ptms_db
spring.datasource.username=postgres
#has?o do bazy danych >
spring.datasource.password=marek123
spring.datasource.driver-class-name=org.postgresql.Driver

# ----------------------------------------------------
# Ustawienia JPA/Hibernate
# ----------------------------------------------------
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true